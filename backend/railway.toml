[build]
builder = "nixpacks"

[deploy]
startCommand = "mkdir -p /tmp/pypath && ln -sf /app /tmp/pypath/backend && PYTHONPATH=/tmp/pypath:/app/research uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
